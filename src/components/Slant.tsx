import type {VFC} from 'react';

const Slant: VFC = () => (
  <div className="relative">
    <div className="w-full bg-gray-100 transform -skew-y-3 -translate-y-1/2 h-[6vw]" />
  </div>
);

export default Slant;
